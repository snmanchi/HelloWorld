var app = angular.module("contactApp", []);
    //.controller("HeaderCtrl", HeaderCtrl)
    //.controller("FooterCtrl", FooterCtrl)
    //.value("AppDataSvc", prepareAppConfig())
    //.value("LoggingSvc", function () {
    //    console.log("Logging Service");
    //})
    //.factory("AppDataFactorySvc", prepareAppConfig) // The Factory service uses function execution that initializes an object and returns 

/* 
function prepareAppConfig(AppNameSvc) {
    var value = {};

    value.name = AppNameSvc;
    value.author = "Sarap";
    value.company = "Angular Service Practice";
    value.version = 1;

    return value;
}

function HeaderCtrl(AppDataSvc, LoggingSvc, AppDataFactorySvc, AppDataServiceSvc) {
    this.appTitle = AppDataServiceSvc.name;
    LoggingSvc();
}
function FooterCtrl(AppDataFactorySvc) {
    this.appTitle = AppDataFactorySvc.name;
    //LoggingSvc();
}
*/

